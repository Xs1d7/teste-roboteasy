# Frontend

Vue 3 + TypeScript + Pinia + SignalR.

## Telas

- `/` landing do produto
- `/login` entrar / cadastrar
- `/users` quem esta online
- `/chat/:userId` conversa + historico

```bash
npm install
npm run dev
```

Proxy local: Auth `:5001`, Chat `:5002`.
