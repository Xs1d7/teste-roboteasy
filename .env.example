# Variaveis locais (copie para .env se quiser)

# Opcional: teste de integracao MessageStore (tests/Chat.Api.Tests)
# Mongo precisa estar acessivel — ex.: docker compose up mongo -d
MONGO_TEST_URL=mongodb://localhost:27017

# Chat (appsettings / compose): Redis para backplane SignalR + presenca
# ConnectionStrings__Redis=localhost:6379
